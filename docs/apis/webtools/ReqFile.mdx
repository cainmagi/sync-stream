---
id: ReqFile
title: ReqFile
sidebar_label: ReqFile
slug: /apis/webtools/ReqFile
description: The possible formats of the fields data in the request.
---

import APITopBar, {IconObjType} from "@site/src/components/APITopBar";

<APITopBar type="type" source="webtools.ReqFile" />

```python
ReqFile = tuple[str, str | bytes, str] | tuple[str, str | bytes]
```

`ReqFile` is a shortcut of a type composition.

`ReqFile` is the possible formats of the `fields` data in the request.
